[package]
name = "alice-monitor"
version = "1.0.0"
edition = "2021"
authors = ["Moroya Sakamoto"]
license = "MIT"
description = "Infrastructure monitoring: health checks, alerts, SLA tracking, uptime, incident management, status pages, heartbeat detection"

[lib]
crate-type = ["rlib"]
